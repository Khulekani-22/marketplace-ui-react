# Vercel Environment Variables for Firebase Configuration
# Copy these to your Vercel project: Settings → Environment Variables

# Frontend Firebase Configuration (VITE_ prefix for Vite)
VITE_FIREBASE_API_KEY=AIzaSyDFzUfv1enm5_lucOMz4tWh26GJfIG751M
VITE_FIREBASE_AUTH_DOMAIN=sloane-hub.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=sloane-hub
VITE_FIREBASE_STORAGE_BUCKET=sloane-hub.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=664957061898
VITE_FIREBASE_APP_ID=1:664957061898:web:71a4e19471132ef7ba88f3

# Backend API Configuration
VITE_API_URL=https://marketplace-firebase.vercel.app/api

# Node Environment
NODE_ENV=production

# Port Configuration (Vercel handles this automatically, but good to define)
PORT=5055

# Backend Firebase Admin SDK (for server-side operations)
# Note: For service account, use Vercel's file upload or secret management
FIREBASE_PROJECT_ID=sloane-hub
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-v5fhd@sloane-hub.iam.gserviceaccount.com

# Optional: If you want to use Firebase Admin SDK with environment variables
# You'll need to add the service account key as a secret
# GOOGLE_APPLICATION_CREDENTIALS=/var/task/secrets/sloane-hub-service-account.json

# CORS Configuration
ALLOWED_ORIGINS=https://marketplace-firebase.vercel.app,http://localhost:3000,http://localhost:5173

# Session/Auth Configuration
SESSION_SECRET=your-secure-session-secret-here-change-this

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Cache Configuration
REDIS_URL=your-redis-url-if-using-upstash-or-vercel-kv

# Analytics (if applicable)
ENABLE_ANALYTICS=true

# Feature Flags
ENABLE_GRAPHQL=true
ENABLE_WEBSOCKETS=false

# Tenant Configuration
DEFAULT_TENANT=vendor
