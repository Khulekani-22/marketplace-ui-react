# Copy to .env and adjust as needed
PORT=5000
JWT_AUDIENCE=marketplace-ui-react
JWT_ISSUER=scdm.local
# For HS256 demo tokens (swap to RSA/ES for Auth0/Firebase in prod)
JWT_SECRET=change-this-in-production

# CORS
CORS_ORIGIN=http://localhost:5173

# Absolute or relative path to the canonical app data JSON
APP_DATA_PATH=../src/data/appData.json

# OpenAI (server reads from environment; never commit real keys)
OPENAI_API_KEY=

